<?php
    $x = 50;
 
    $x += 10; // Exactly the same as: $x = $x + 10; (Result: 60)
    $x -= 20; // Exactly the same as: $x = $x - 20; (Result: 40)
    $x *= 2;  // Exactly the same as: $x = $x * 2;  (Result: 80)
    $x /= 4;  // Exactly the same as: $x = $x / 4;  (Result: 20)
?>